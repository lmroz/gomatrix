"make test" will compile and test functionality.

"make install" will install this package in your system.

"make examples" will compile a program "examples" which demonstrates some basic matrix operations.
